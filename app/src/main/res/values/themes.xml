<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Books" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:typeface">monospace</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/primary_background_color</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/foreground_launcher_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.Books</item>
    </style>

    <style name="BooksEditText">
        <item name="colorControlActivated">@color/primary_text_color</item>
        <item name="android:textCursorDrawable">@drawable/drawable_text_cursor</item>
    </style>
</resources>